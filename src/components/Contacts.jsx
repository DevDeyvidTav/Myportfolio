// assets



// contexts
import { useContext } from "react";
import { PortfolioContext } from "../contexts/PortfolioContext";





export function Contacts() {
    const { open } = useContext(PortfolioContext)
    return (
        <div id="Contact" className="h-screen w-screen bg-blue-600">
            
        </div>
    )
}